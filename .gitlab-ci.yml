#image: djerfy/gitlab-ci-perl:latest

before_script:
 - echo "New job started"

after_script:
 - echo "Job just finished"
 - ls -la .

#before_script:
#  - echo "Before script installation"
#  - yum update
#  - yum install perl-Devel-Cover perl-JSON-XS -y

stages:
  - test

test:unit:
  stage: test
  tags:
    - perl
  before_script:
    - echo "Starting test:unit:"
  after_script:
   - echo "Finished test:unit:"
  script:
    - perl --version
    - perl -c

#perl_version:
#  tags:
#    - perl
#  script:
#    - perl --version
    